document.querySelector("main").remove()

const newHeader = document.createElement('h1')

newHeader.id ="victory"

const championName = "Julian Victorio"

newHeader.innerHTML = `${championName} is the champion`